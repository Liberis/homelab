---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: adguard-exporter
resources:
  - namespace.yaml
  - deployment.yaml
  - servicemonitor.yaml
configMapGenerator:
  - name: adguard-dashboard
    options:
      disableNameSuffixHash: true
      labels:
        grafana_dashboard: "1"
    files:
      - dashboards/adguard.json
